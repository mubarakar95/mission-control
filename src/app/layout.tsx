import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/infrastructure/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Mission Control - OpenClaw AI",
    template: "%s | Mission Control",
  },
  description: "Comprehensive dashboard for managing OpenClaw AI agents, tasks, scheduling, and monitoring.",
  keywords: ["OpenClaw", "AI", "Agent", "Dashboard", "Mission Control", "Automation", "CLI", "Gateway"],
  authors: [{ name: "OpenClaw Team" }],
  icons: {
    icon: "/logo.svg",
  },
  openGraph: {
    title: "Mission Control - OpenClaw AI",
    description: "AI Agent Dashboard & GUI",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
